services:
  llama-server:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    # entrypoint: ["sleep", "infinity"]
     # -m /models/sarvam-translate.Q3_K_M.gguf
     # -m /models/Apertus-8B-2509.Q4_K_M.gguf
     # -m ${MODEL_PATH}
    command: >
      -m /models/TowerInstruct-Mistral-7B-v0.2.Q6_K.gguf
      --port 8080
      --host 0.0.0.0
      -n 2048
      -fa on
    volumes:
      - ./models:/models
    ports:
      - "8080:8080"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
              #device_ids: ['0']
